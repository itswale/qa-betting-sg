# Copy to .env and adjust. Used by pytest (conftest loads this file).

BASE_URL=https://qae-assignment-tau.vercel.app
USER_ID=candidate-Z1tPv7cRx5K

# Browser: headed window when true; omit or false for headless (default).
# Still overridden by CLI: pytest --headed always forces headed.
PLAYWRIGHT_HEADED=false
